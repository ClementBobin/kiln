{
  "name": "Enum Extensions",
  "description": "Adds a GetEnumDescription() extension method that reads [Description] attributes off enum members.",
  "tags": ["dotnet", "tools", "enum"],

  "variables": [
    { "key": "root_namespace", "label": "Root namespace", "default": "Api" }
  ],

  "apply": {
    "type": "local"
  }
}