import { H as HeadlessOptions } from '../cli-CgIzEp0u.js';

/**
 * run.ts — Headless (non-interactive) scaffold command.
 * Used by: kiln run --config-id react/vite --var project_name=my-app
 */

declare function runHeadless(opts: HeadlessOptions): Promise<void>;

export { runHeadless };
